{
  "name": "nour-landing-page",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-structure.mjs",
    "preview:checkout": "node scripts/preview-checkout-mock.mjs"
  },
  "dependencies": {
    "@netlify/database": "2.0.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
